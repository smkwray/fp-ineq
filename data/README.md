# Data Contract

`data/series/` contains normalized quarterly `period,value` CSVs.

`data/reports/` contains provenance JSON with source name, source URL, units, coverage, and refresh metadata.

Runtime `*.DAT` files are generated into ignored `runtime/overlay_stock_fm/` paths and are not part of the public repo.

